# Optional: if omitted in production, the Vercel rewrite will proxy /api/token to Render.
# You can set either the backend root URL or the full /api/token URL.
VITE_TOKEN_API_BASE_URL=https://your-backend-domain.onrender.com
